Low-level packaging of axios

### Installation
```
npm install

```

### Usage
```
import axios-low from 'axios-low-encapsulation'

const response = axios.get(url, data, params)

const response = axios.post(url, data, params)

```
